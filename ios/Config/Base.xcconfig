// Placeholders only. The household's values go in Local.xcconfig, which is git-ignored;
// copy Local.example.xcconfig to start one.
DEVELOPMENT_TEAM =
BUNDLE_ID_PREFIX = org.example
APP_HOST = localhost
CODE_SIGN_STYLE = Automatic

#include? "Local.xcconfig"
