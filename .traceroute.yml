---
# Active Storage ships its own engine, routes and controllers. Traceroute walks every loaded
# controller and every mounted route, so the engine's own pairs look like an app that forgot
# to route its actions. They are not ours to route, to reach, or to delete.
ignore_unreachable_actions:
  - ^active_storage
ignore_unused_routes:
  - ^active_storage
